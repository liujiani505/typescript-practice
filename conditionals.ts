// if statement

let cheese
const value:string = "cheap"

if(value === "expensive") {
    cheese = "manchego"
}

console.log(cheese)

// checking if a variable has a value
// everything is true expect false, 0, "", undefined, null, NaN
if(!cheese) {
    console.log("You have no cheese")
}